import { Button } from '../types/buttons';
import { BTN_RENAME_TERMINAL } from '../constants/buttons';

const buttons: Button[] = [
  {
    command: BTN_RENAME_TERMINAL,
    text: 'btn.rename',
    tooltip: 'btn.rename.tooltip',
  },
];

export default buttons;
